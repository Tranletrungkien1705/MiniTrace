# DEEPEN-LOG — MiniTrace

- 2026-09-23 | feat: chống hàng giả — xác thực quét mã (VerifyCount/IP/GPS/SĐT) + phát hiện nghi giả khi quét nhiều nơi hoặc sau khi đã bán. Port từ InBrandCloud `Inv_InventoryVerifiedID` (doc 09 §9). Thêm entity `Verification` + enum `VerifyStatus`, `TraceService.VerifyAsync/VerificationsAsync`, API `POST /api/verify` (công khai) + `POST /api/v1/verify` + `GET /api/v1/verifications`, seed mẫu, trang SPA "Chống hàng giả". Build Release 0 error; smoke test: quét lần 3 ở địa điểm khác → status=2 "Nghi hàng giả". Commit 360dcd7, đã push origin/main.
- 2026-09-23 | feat: danh mục sự kiện truy xuất trọng yếu (GS1 Critical Tracking Event — CTE). Port từ InBrandCloud eTEM `Mst_CTE` (doc 09 §2.2/§3, `eTemNN.cs` Mst_CTE_Create/Update/Delete). Thêm entity `Cte` (Code/Description/NetworkType/ApiLink/Active) + DbSet + query filter tenant, `TraceService.CtesAsync/SaveCteAsync/DeleteCteAsync` (validate mã+diễn giải, chống trùng mã theo tenant), API `GET/POST /api/v1/ctes` + `DELETE /api/v1/ctes/{id}`, seed 8 sự kiện GS1 (PRODUCTION_IN…CONSUMER_SCAN), trang SPA "Sự kiện (CTE)" (list + form thêm/sửa/xóa). Build Release 0 error; smoke test: list=8, create/update/delete OK, trùng mã → 400 "Mã 'EXPORT_CUSTOMS' đã tồn tại.". Commit e1f785f, đã push origin/main.
- 2026-09-23 | feat: danh mục thành phần dữ liệu trọng yếu (GS1 Key Data Element — KDE) + ánh xạ sự kiện↔thành phần (CTE_KDE). Port từ InBrandCloud eTEM `Mst_KDE` + `CTE_KDE` (doc 09 §2.2/§3, `eTemNN.cs` Mst_KDE_Create/Update/Delete + CTE_KDE_Save/CTE_KDE_SaveX). Thêm entity `Kde` (Code/Description/DataType/RefNoList/NetworkType/FlagList/FlagQuery/Active) + `CteKde` (CteCode/KdeCode/NetworkType/FlagKey/FlagOsOrgView) + DbSet + query filter tenant, `TraceService.KdesAsync/SaveKdeAsync/DeleteKdeAsync` + `CteKdesAsync/SaveCteKdesAsync` (áp 2 quy tắc nghiệp vụ InBrandCloud: mỗi sự kiện tối đa 1 KDE loại danh sách FlagList; phải có ít nhất 1 KDE là Key FlagKey; chặn xóa KDE đang được ánh xạ), API `GET/POST /api/v1/kdes` + `DELETE /api/v1/kdes/{id}` + `GET/POST /api/v1/cte-kdes`, seed 10 KDE GS1 (LOT_NO/PROD_DATE/EXP_DATE/SERIAL_NO/QUANTITY/FROM_LOCATION/TO_LOCATION/CARRIER/STORAGE_TEMP/QUALITY_RESULT) + 14 ánh xạ CTE_KDE, trang SPA "Thành phần (KDE)" (list + form thêm/sửa/xóa + modal ánh xạ vào sự kiện). Build Release 0 error; SPA vite build OK; smoke test: kdes=10, PRODUCTION_IN có 3 KDE, lưu không Key → 400 "Mỗi sự kiện phải có ít nhất 1 thành phần là Key (FlagKey).", 2 KDE loại danh sách → 400 "Mỗi sự kiện chỉ được có tối đa 1 thành phần loại danh sách…", xóa KDE đang dùng → 400 "đang được dùng trong sự kiện — gỡ ánh xạ trước.".
- 2026-09-23 | feat: danh mục địa điểm toàn cầu (GS1 Global Location Number — GLN). Port từ InBrandCloud eTEM `Mst_GLN` (doc 09 §2.1/§3, `eTemNN.cs` Mst_GLN_Create/Update/Delete/Get + Mst_GLN_CheckDB). Thêm entity `Gln` (Code/Name/GpsLat/GpsLong/Remark/Active) + DbSet + query filter tenant, `TraceService.GlnsAsync/SaveGlnAsync/DeleteGlnAsync` (validate mã+tên bắt buộc, chống trùng mã theo tenant), API `GET/POST /api/v1/glns` + `DELETE /api/v1/glns/{id}`, seed 4 địa điểm chuỗi cung ứng mẫu (nhà máy/kho/đại lý/siêu thị kèm toạ độ GPS), trang SPA "Địa điểm (GLN)" (list + form thêm/sửa/xóa). Build Release 0 error; SPA vite build OK. Commit b5ff153, đã push origin/main.
- 2026-09-23 | feat: mẫu loại tổ chức (GS1 Network Type Template). Port từ InBrandCloud eTEM `Mst_TemplateNWType` + `TplNWT_Mst_CTE` + `TplNWT_Mst_KDE` + `TplNWT_CTE_KDE` (doc 09 §2.1/§3, `MstTemplate.cs` Mst_TemplateNWType_Save/SaveX/GetX + `Mst_TemplateNWType.cs`/`TplNWT_*.cs`). Thêm entity `TemplateNWType` (TplNWType/Description/Status/Remark) + enum `TplNwtStatus` (Pending/Approve/Cancel) + `TplNwtCte`/`TplNwtKde`/`TplNwtCteKde` + DbSet + query filter tenant, `TraceService.TemplatesAsync/GetTemplateAsync/SaveTemplateAsync/DeleteTemplateAsync/ApproveTemplateAsync` (áp quy tắc InBrandCloud: cần mã+tên, phải có ≥1 CTE và ≥1 KDE, mẫu đã duyệt không cho sửa, CTE/KDE/ánh xạ phải tồn tại trong danh mục), API `GET/POST /api/v1/templates` + `GET /api/v1/templates/{id}` + `POST /api/v1/templates/{id}/approve` + `DELETE /api/v1/templates/{id}`, seed 2 mẫu (MANUFACTURER đã duyệt, DISTRIBUTOR chờ duyệt), trang SPA "Mẫu loại tổ chức" (list + form chọn CTE/KDE + lưới ánh xạ CTE↔KDE có cờ Key/Ngoài-org). Build Release 0 error; SPA vite build OK.
- 2026-09-23 | feat: mẫu hiển thị sự kiện truy xuất (GS1 Template View Event). Port từ InBrandCloud eTEM `Mst_TplViewEvent` (doc 09 §7, `MstTemplate.cs` Mst_TplViewEvent_Create/CreateX/Update/Delete + Mst_TplViewEvent_CheckDB). Thêm entity `TplViewEvent` (Code/TplVEDesc/Detail/TplVEDetail/CteCode/Remark/Active/FlagBG) + DbSet + query filter tenant, `TraceService.TplViewEventsAsync/SaveTplViewEventAsync/DeleteTplViewEventAsync` (áp quy tắc InBrandCloud: cần mã+mô tả+chi tiết, mã duy nhất theo tenant, CTE tham chiếu phải tồn tại, mỗi sự kiện tối đa 1 mẫu hiển thị đang hoạt động), API `GET/POST /api/v1/tpl-view-events` + `DELETE /api/v1/tpl-view-events/{id}`, seed 5 mẫu (VE_PRODUCTION/VE_QUALITY/VE_SHIPMENT/VE_RETAIL + VE_DEFAULT nền), trang SPA "Mẫu hiển thị" (list + form thêm/sửa/xóa). Build Release 0 error; SPA vite build OK.
