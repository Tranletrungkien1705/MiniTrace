# DEEPEN-LOG — MiniTrace

- 2026-09-23 | feat: chống hàng giả — xác thực quét mã (VerifyCount/IP/GPS/SĐT) + phát hiện nghi giả khi quét nhiều nơi hoặc sau khi đã bán. Port từ InBrandCloud `Inv_InventoryVerifiedID` (doc 09 §9). Thêm entity `Verification` + enum `VerifyStatus`, `TraceService.VerifyAsync/VerificationsAsync`, API `POST /api/verify` (công khai) + `POST /api/v1/verify` + `GET /api/v1/verifications`, seed mẫu, trang SPA "Chống hàng giả". Build Release 0 error; smoke test: quét lần 3 ở địa điểm khác → status=2 "Nghi hàng giả". Commit 360dcd7, đã push origin/main.
- 2026-09-23 | feat: danh mục sự kiện truy xuất trọng yếu (GS1 Critical Tracking Event — CTE). Port từ InBrandCloud eTEM `Mst_CTE` (doc 09 §2.2/§3, `eTemNN.cs` Mst_CTE_Create/Update/Delete). Thêm entity `Cte` (Code/Description/NetworkType/ApiLink/Active) + DbSet + query filter tenant, `TraceService.CtesAsync/SaveCteAsync/DeleteCteAsync` (validate mã+diễn giải, chống trùng mã theo tenant), API `GET/POST /api/v1/ctes` + `DELETE /api/v1/ctes/{id}`, seed 8 sự kiện GS1 (PRODUCTION_IN…CONSUMER_SCAN), trang SPA "Sự kiện (CTE)" (list + form thêm/sửa/xóa). Build Release 0 error; smoke test: list=8, create/update/delete OK, trùng mã → 400 "Mã 'EXPORT_CUSTOMS' đã tồn tại.". Commit e1f785f, đã push origin/main.
- 2026-09-23 | feat: danh mục thành phần dữ liệu trọng yếu (GS1 Key Data Element — KDE) + ánh xạ sự kiện↔thành phần (CTE_KDE). Port từ InBrandCloud eTEM `Mst_KDE` + `CTE_KDE` (doc 09 §2.2/§3, `eTemNN.cs` Mst_KDE_Create/Update/Delete + CTE_KDE_Save/CTE_KDE_SaveX). Thêm entity `Kde` (Code/Description/DataType/RefNoList/NetworkType/FlagList/FlagQuery/Active) + `CteKde` (CteCode/KdeCode/NetworkType/FlagKey/FlagOsOrgView) + DbSet + query filter tenant, `TraceService.KdesAsync/SaveKdeAsync/DeleteKdeAsync` + `CteKdesAsync/SaveCteKdesAsync` (áp 2 quy tắc nghiệp vụ InBrandCloud: mỗi sự kiện tối đa 1 KDE loại danh sách FlagList; phải có ít nhất 1 KDE là Key FlagKey; chặn xóa KDE đang được ánh xạ), API `GET/POST /api/v1/kdes` + `DELETE /api/v1/kdes/{id}` + `GET/POST /api/v1/cte-kdes`, seed 10 KDE GS1 (LOT_NO/PROD_DATE/EXP_DATE/SERIAL_NO/QUANTITY/FROM_LOCATION/TO_LOCATION/CARRIER/STORAGE_TEMP/QUALITY_RESULT) + 14 ánh xạ CTE_KDE, trang SPA "Thành phần (KDE)" (list + form thêm/sửa/xóa + modal ánh xạ vào sự kiện). Build Release 0 error; SPA vite build OK; smoke test: kdes=10, PRODUCTION_IN có 3 KDE, lưu không Key → 400 "Mỗi sự kiện phải có ít nhất 1 thành phần là Key (FlagKey).", 2 KDE loại danh sách → 400 "Mỗi sự kiện chỉ được có tối đa 1 thành phần loại danh sách…", xóa KDE đang dùng → 400 "đang được dùng trong sự kiện — gỡ ánh xạ trước.".
- 2026-09-23 | feat: danh mục địa điểm toàn cầu (GS1 Global Location Number — GLN). Port từ InBrandCloud eTEM `Mst_GLN` (doc 09 §2.1/§3, `eTemNN.cs` Mst_GLN_Create/Update/Delete/Get + Mst_GLN_CheckDB). Thêm entity `Gln` (Code/Name/GpsLat/GpsLong/Remark/Active) + DbSet + query filter tenant, `TraceService.GlnsAsync/SaveGlnAsync/DeleteGlnAsync` (validate mã+tên bắt buộc, chống trùng mã theo tenant), API `GET/POST /api/v1/glns` + `DELETE /api/v1/glns/{id}`, seed 4 địa điểm chuỗi cung ứng mẫu (nhà máy/kho/đại lý/siêu thị kèm toạ độ GPS), trang SPA "Địa điểm (GLN)" (list + form thêm/sửa/xóa). Build Release 0 error; SPA vite build OK. Commit b5ff153, đã push origin/main.
- 2026-09-23 | feat: mẫu loại tổ chức (GS1 Network Type Template). Port từ InBrandCloud eTEM `Mst_TemplateNWType` + `TplNWT_Mst_CTE` + `TplNWT_Mst_KDE` + `TplNWT_CTE_KDE` (doc 09 §2.1/§3, `MstTemplate.cs` Mst_TemplateNWType_Save/SaveX/GetX + `Mst_TemplateNWType.cs`/`TplNWT_*.cs`). Thêm entity `TemplateNWType` (TplNWType/Description/Status/Remark) + enum `TplNwtStatus` (Pending/Approve/Cancel) + `TplNwtCte`/`TplNwtKde`/`TplNwtCteKde` + DbSet + query filter tenant, `TraceService.TemplatesAsync/GetTemplateAsync/SaveTemplateAsync/DeleteTemplateAsync/ApproveTemplateAsync` (áp quy tắc InBrandCloud: cần mã+tên, phải có ≥1 CTE và ≥1 KDE, mẫu đã duyệt không cho sửa, CTE/KDE/ánh xạ phải tồn tại trong danh mục), API `GET/POST /api/v1/templates` + `GET /api/v1/templates/{id}` + `POST /api/v1/templates/{id}/approve` + `DELETE /api/v1/templates/{id}`, seed 2 mẫu (MANUFACTURER đã duyệt, DISTRIBUTOR chờ duyệt), trang SPA "Mẫu loại tổ chức" (list + form chọn CTE/KDE + lưới ánh xạ CTE↔KDE có cờ Key/Ngoài-org). Build Release 0 error; SPA vite build OK.
- 2026-09-23 | feat: mẫu hiển thị sự kiện truy xuất (GS1 Template View Event). Port từ InBrandCloud eTEM `Mst_TplViewEvent` (doc 09 §7, `MstTemplate.cs` Mst_TplViewEvent_Create/CreateX/Update/Delete + Mst_TplViewEvent_CheckDB). Thêm entity `TplViewEvent` (Code/TplVEDesc/Detail/TplVEDetail/CteCode/Remark/Active/FlagBG) + DbSet + query filter tenant, `TraceService.TplViewEventsAsync/SaveTplViewEventAsync/DeleteTplViewEventAsync` (áp quy tắc InBrandCloud: cần mã+mô tả+chi tiết, mã duy nhất theo tenant, CTE tham chiếu phải tồn tại, mỗi sự kiện tối đa 1 mẫu hiển thị đang hoạt động), API `GET/POST /api/v1/tpl-view-events` + `DELETE /api/v1/tpl-view-events/{id}`, seed 5 mẫu (VE_PRODUCTION/VE_QUALITY/VE_SHIPMENT/VE_RETAIL + VE_DEFAULT nền), trang SPA "Mẫu hiển thị" (list + form thêm/sửa/xóa). Build Release 0 error; SPA vite build OK.
- 2026-09-23 | feat: danh mục nông trại / vùng trồng (GS1 Farm). Port từ InBrandCloud eTEM `Mst_Farm` (doc 09 §2.1/§3, `eTemNN.cs` Mst_Farm_GetX/Get + Mst_Farm_CheckDB + `MstFarmController.cs` WA_Mst_Farm_Get). Thêm entity `Farm` (Code/FarmName/NetworkType/Active) + DbSet + query filter tenant, `TraceService.FarmsAsync/SaveFarmAsync/DeleteFarmAsync` (validate mã+tên bắt buộc, chống trùng mã theo tenant), API `GET/POST /api/v1/farms` + `DELETE /api/v1/farms/{id}`, seed 3 nông trại mẫu (Sóc Trăng/Cần Thơ/Đồng Tháp), trang SPA "Nông trại" (list + form thêm/sửa/xóa). Build Release 0 error; SPA vite build OK.
- 2026-09-23 | feat: sự kiện truy xuất theo CTE + KDE (GS1 Event_Event + Event_EventSpec). Port từ InBrandCloud eTEM `Event_Event` + `Event_EventSpec` (doc 09 §4/§5, `Event.cs` Event_Event_Save/SaveX + `Event_Event.cs`/`Event_EventSpec.cs`). Thêm entity `TraceRecord` (EventNo/CTECode/TplVECode/TplVEDetail/UIStyleCode/GLNOrgCode/GLNOrgName/GPSLat/GPSLong/Remark/Active) + `TraceRecordSpec` (CTECode/KDECode/KDEValue/FlagKey/FlagList/FlagOsOrgView) + DbSet + query filter tenant, `TraceService.RecordsAsync/GetRecordAsync/SaveRecordAsync/DeleteRecordAsync` (áp 5 quy tắc InBrandCloud: CTE phải tồn tại; mọi KDE phải nằm trong ánh xạ CTE_KDE; tối đa 1 KDE loại danh sách; mọi KDE Key phải có giá trị; bộ giá trị KDE Key tạo "dấu vân tay" EventNo → ghi lại cùng bộ Key thì CẬP NHẬT bản ghi cũ thay vì tạo mới), snapshot mẫu hiển thị (TplViewEvent) + địa điểm (GLN) khi ghi, API `GET/POST /api/v1/records` + `GET/DELETE /api/v1/records/{id}`, seed 2 bản ghi mẫu (PRODUCTION_IN 3 KDE Key, QUALITY_CHECK 1 KDE danh sách), trang SPA "Sự kiện truy xuất" (list + form chọn CTE/GLN + lưới nhập giá trị KDE theo ánh xạ). Build Release 0 error 0 warning; SPA vite build OK.
- 2026-09-23 | feat: ánh xạ tổ chức ↔ địa điểm (GS1 Mst_OrgIDMapGLN). Port từ InBrandCloud eTEM `Mst_OrgIDMapGLN` (doc 09 §2.1/§3, `eTemNN.cs` Mst_OrgIDMapGLN_GetX/Get + `Mst_OrgIDMapGLN.cs` model + `MstOrgIDMapGLNController.cs` WA_Mst_OrgIDMapGLN_Get). Thêm entity `OrgGln` (OrgCode/GlnCode/Remark) + DbSet + query filter tenant, `TraceService.OrgGlnsAsync/SaveOrgGlnAsync/DeleteOrgGlnAsync` (join sang danh mục GLN để làm giàu tên địa điểm + toạ độ GPS — tương đương left join Mst_GLN; áp quy tắc InBrandCloud: cần mã tổ chức + mã địa điểm, địa điểm phải tồn tại trong danh mục GLN, cặp (OrgID, GLNCode) duy nhất trong tenant), API `GET/POST /api/v1/org-glns` + `DELETE /api/v1/org-glns/{id}`, seed 4 ánh xạ mẫu (nhà sản xuất→nhà máy+kho, đại lý→đại lý cấp 1, điểm bán lẻ→siêu thị), trang SPA "Tổ chức ↔ Địa điểm" (list + form chọn GLN). Build Release 0 error 0 warning; SPA vite build OK.
- 2026-09-23 | feat: danh mục kiểu dữ liệu (GS1 Data Type — Mst_DataType). Port từ InBrandCloud eTEM `Mst_DataType` (doc 09 §2.2/§3, `eTemNN.cs` Mst_DataType_CheckDB/GetX/Get + `Mst_DataType.cs` model). Thêm entity `DataType` (Code/Description/NetworkType/Active) + DbSet + query filter tenant, `TraceService.DataTypesAsync/SaveDataTypeAsync/DeleteDataTypeAsync` (áp quy tắc InBrandCloud: cần mã kiểu + diễn giải, mã duy nhất theo tenant, chặn xóa kiểu đang được KDE tham chiếu — tương đương Mst_DataType_CheckDB khi tạo KDE), API `GET/POST /api/v1/data-types` + `DELETE /api/v1/data-types/{id}`, seed 4 kiểu GS1 (Text/Number/Date/List), trang SPA "Kiểu dữ liệu" (list + form thêm/sửa/xóa). Build Release 0 error 0 warning; SPA vite build OK.
- 2026-09-23 | feat: sinh tem / kho số tem (GS1 Inv_GenTimes + Inv_InventoryGenID). Port từ InBrandCloud eTEM `Inv_GenTimes` + `Inv_InventoryGenID` (doc 09 §3.1/§3.2, `InvGen.cs` Inv_GenTimes_AddX_New20211125 + `Inv_InventoryGenID.cs` model + `InvInventoryGenIDController.cs` WA_Inv_InventoryGenID_Get + `ElasticSearch.eTEM.cs` Inv_InventoryGenID_HashMD5). Thêm entity `StampBatch` (GenTimesNo/ProductCode/ProductName/QRType/ConfigDomain/Qty/FlagPIN/FlagMap/ProductionLotNo/ProductionDate/ShiftInCode/UserKCS/Remark) + enum `QrType` (ProdId/Box/Carton/Tem) + `Stamp` (IDNo/QR_ID/PIN/SecretNo/HashInformation/FlagMap/FlagUsed) + DbSet + query filter tenant, `TraceService.StampBatchesAsync/GetStampBatchAsync/GenerateStampsAsync/DeleteStampBatchAsync/StampsAsync` (áp quy tắc InBrandCloud: cần mã lần sinh + mã hàng hoá; Qty 1–100.000 tương đương nQtyMaxGen; mã lần sinh duy nhất theo tenant; tem PRODID + FlagPIN sinh kèm PIN bí mật và HashInformation = MD5(IDNo|PIN) chống giả), API `GET/POST /api/v1/stamp-batches` + `GET/DELETE /api/v1/stamp-batches/{id}` + `GET /api/v1/stamps`, seed 1 lần sinh tem mẫu (5 tem PRODID có PIN + Hash), trang SPA "Sinh tem" (list + form sinh tem + modal xem danh sách tem). Build Release 0 error 0 warning; SPA vite build OK.
- 2026-09-23 | feat: đóng hộp / gán tem vào hộp (GS1 Inv_InventoryGenBox + Map_IDInBox). Port từ InBrandCloud eTEM `Inv_InventoryGenBox` + `Map_IDInBox` (doc 09 §3.3 hierarchy Thùng→Hộp→Sản phẩm, `InvGen.cs` Map_IDInBox_Add_New20211125/Map_IDInBox_AddX_New20211125 + `Inv_InventoryGenBox.cs`/`Map_IDInBox.cs` model). Thêm entity `Box` (BoxNo/QR_BoxNo/GenTimesNo/ProductCode/ProductName/Remark/FlagMap/FlagUsed) + `BoxItem` (BoxNo/IDNo/ProductCode/InvCode/FlagActive) + DbSet + query filter tenant, `TraceService.BoxesAsync/GetBoxAsync/CreateBoxAsync/AddStampsToBoxAsync/DeleteBoxAsync` (áp 3 quy tắc InBrandCloud: mã hộp duy nhất theo tenant; mọi IDNo phải tồn tại trong kho số tem Inv_InventoryGenID; mỗi tem chỉ nằm trong MỘT hộp — chống gán trùng), API `GET/POST /api/v1/boxes` + `GET /api/v1/boxes/{id}` + `POST /api/v1/boxes/{id}/stamps` + `DELETE /api/v1/boxes/{id}`, seed 1 hộp mẫu gom 3 tem (P000001–P000003), trang SPA "Đóng hộp" (list + form tạo hộp + modal xem tem trong hộp & gán tem). Build Release 0 error 0 warning; SPA vite build OK.
- 2026-09-23 | feat: hàng đợi đồng bộ dữ liệu truy xuất (GS1 MstSv_QueSync). Port từ InBrandCloud eTEM `MstSv_QueSync` (doc 06 §6.2 luồng đồng bộ eTEM, `SyncEtemNN.cs` MstSv_QueSync_Create/CreateX + MstSv_QueSync_CheckDB + `MstSv_QueSync.cs` model). Thêm entity `QueSync` (NetworkId/QueSyncNo/TableCode/FlagSync/FlagSyncBL/Status/RetryCount/ErrorDetail/SyncedAt/Remark) + enum `QueSyncStatus` (Pending/Synced/Failed) + DbSet + query filter tenant, `TraceService.QueSyncsAsync/SaveQueSyncAsync/MarkQueSyncAsync/DeleteQueSyncAsync` (áp quy tắc InBrandCloud: cần đủ NetworkID+QueSyncNo+TableCode; bộ ba duy nhất trong tenant — chống đẩy trùng; bản ghi mới/lưu lại quay về trạng thái chờ đồng bộ; đánh dấu Synced tắt FlagSync + ghi SyncedAt, Failed tăng RetryCount + lưu ErrorDetail), API `GET/POST /api/v1/que-syncs` + `POST /api/v1/que-syncs/{id}/mark` + `DELETE /api/v1/que-syncs/{id}`, seed 4 bản ghi mẫu (2 chờ đồng bộ, 1 đã đồng bộ + blockchain, 1 lỗi retry=2), trang SPA "Hàng đợi đồng bộ" (list + form thêm/sửa/xóa + nút đánh dấu Đã đồng bộ/Báo lỗi). Build Release 0 error 0 warning; SPA vite build OK. Commit 9881bba, đã push origin/main.
- 2026-09-23 | feat: danh mục dữ liệu gốc (GS1 Master Data — Mst_MasterData). Port từ InBrandCloud eTEM `Mst_MasterData` (doc 09 §2.2/§3, `eTemNN.cs` Mst_MasterData_CheckDB/GetX/Get + `Mst_MasterData.cs` model + `MstMasterDataController.cs` WA_Mst_MasterData_Get). Thêm entity `MasterData` (Code/NetworkId/TableName/Active/Remark) + DbSet + query filter tenant, `TraceService.MasterDatasAsync/SaveMasterDataAsync/DeleteMasterDataAsync` (áp quy tắc InBrandCloud: cần mã danh mục MDCode + tên bảng TableName, mã danh mục duy nhất trong tenant), API `GET/POST /api/v1/master-datas` + `DELETE /api/v1/master-datas/{id}`, seed 5 danh mục mẫu (MD_CTE/MD_KDE/MD_GLN/MD_FARM + MD_EVENT tạm ngưng), trang SPA "Dữ liệu gốc" (list + form thêm/sửa/xóa). Build Release 0 error 0 warning; SPA vite build OK. Commit 5c380e3, đã push origin/main.
- 2026-09-23 | feat: tổ chức tham gia mạng lưới truy xuất (GS1 Network Organization — Mst_NNT). Port từ InBrandCloud eTEM `Mst_NNT` + luồng `Mst_NNT_QueSync` (doc 09 §2.1/§5, `eTemNN.cs` Mst_NNT_QueSync + `Master.cs` Mst_NNT_CheckDB + `MasterData.Org.cs` Mst_Org_CheckDB + `Seq.cs` Seq_GenObjCode_V1_GetX + `SyncEtemNN.cs` QueSync_Mst_NNT_Perform + `QueSync_Mst_NNT.cs` model). Thêm entity `NetworkOrg` (Mst/FullName/NetworkType/OrgCode/Address/Mobile/ContactName/ContactEmail/Gln/EltsMstId/Status/Active/Remark) + enum `NetworkOrgStatus` (New/Approved/Rejected) + DbSet + query filter tenant, `TraceService.NetworkOrgsAsync/SaveNetworkOrgAsync/DeleteNetworkOrgAsync/RegisterNetworkOrgAsync` (áp quy tắc InBrandCloud: cần MST + tên đầy đủ, MST duy nhất trong tenant; đăng ký mạng chỉ cho tổ chức đang hoạt động — tương đương Mst_NNT_CheckDB FlagActive; cấp mã định danh ngoài mạng ELTSMSTId nếu chưa có — tương đương Seq_GenObjCode_V1_GetX; đưa vào hàng đợi đồng bộ QueSync với TableCode=Mst_NNT chống đẩy trùng — tương đương QueSync_Mst_NNT_Perform; đánh dấu RegisterStatus=APPROVED), API `GET/POST /api/v1/network-orgs` + `POST /api/v1/network-orgs/{id}/register` + `DELETE /api/v1/network-orgs/{id}`, seed 3 tổ chức mẫu (nhà sản xuất đã duyệt mạng, đại lý chờ đăng ký, điểm bán lẻ tạm ngưng), trang SPA "Tổ chức mạng" (list + form thêm/sửa/xóa + nút Đăng ký mạng). Build Release 0 error 0 warning; SPA vite build OK.
- 2026-09-23 | feat: đóng thùng / gán hộp vào thùng (GS1 Inv_InventoryGenCarton + Map_BoxInCarton). Port từ InBrandCloud eTEM `Inv_InventoryGenCarton` + `Map_BoxInCarton` (doc 09 §3.3 hierarchy Thùng→Hộp→Sản phẩm, `InvGen.cs` Inv_GenTimes_AddX_New20211125 nhánh QRType=Carton + `Inv_InventoryGenCarton.cs`/`Inv_InventoryCarton.cs` model). Thêm entity `Carton` (CanNo/QR_CanNo/GenTimesNo/ProductCode/ProductName/Remark/FlagMap/FlagUsed) + `CartonItem` (CanNo/BoxNo/ProductCode/InvCode/FlagActive) + DbSet + query filter tenant, `TraceService.CartonsAsync/GetCartonAsync/CreateCartonAsync/AddBoxesToCartonAsync/DeleteCartonAsync` (áp 3 quy tắc InBrandCloud: mã thùng duy nhất theo tenant; mọi BoxNo phải tồn tại trong kho số hộp Inv_InventoryGenBox; mỗi hộp chỉ nằm trong MỘT thùng — chống gán trùng), API `GET/POST /api/v1/cartons` + `GET /api/v1/cartons/{id}` + `POST /api/v1/cartons/{id}/boxes` + `DELETE /api/v1/cartons/{id}`, seed 1 thùng mẫu gom hộp B2601010001, trang SPA "Đóng thùng" (list + form tạo thùng + modal xem hộp trong thùng & gán hộp). Build Release 0 error 0 warning; SPA vite build OK. Commit 5ba5c82, đã push origin/main.
- 2026-09-23 | feat: kho số bí mật (GS1 Secret Inventory — Inv_InventorySecret). Port từ InBrandCloud eTEM `Inv_InventorySecret` (doc 09 §3.1/§9 chống hàng giả, `Inv_InventorySecret.cs` model + `Inv_InventorySecretUI.cs` + `Inv_InventorySecretController.cs` WA_Inv_InventorySecret_Get/UpdateFlagUsed + `Inv_InventorySecretService.cs` + `InventoryForm.cs` nhánh Inv_InventorySecret:FlagMap). Thêm entity `Secret` (SerialNo/NetworkId/Mst/OrgCode/GenTimesNo/SecretNo/QR_SerialNo/FlagMap/FlagUsed/Remark) + DbSet + query filter tenant, `TraceService.SecretsAsync/SaveSecretAsync/DeleteSecretAsync/MarkSecretUsedAsync` (áp quy tắc InBrandCloud: cần SerialNo + SecretNo, SecretNo duy nhất trong tenant; phát hành/dùng đánh dấu FlagUsed — tương đương WA_Inv_InventorySecret_UpdateFlagUsed, chặn phát hành lại số đã dùng), API `GET/POST /api/v1/secrets` + `POST /api/v1/secrets/{id}/use` + `DELETE /api/v1/secrets/{id}`, seed 4 số bí mật mẫu (2 chưa dùng, 1 đã phát hành, 1 chưa ghép serial), trang SPA "Số bí mật" (list + lọc theo trạng thái dùng + form thêm/sửa/xóa + nút Phát hành). Build Release 0 error 0 warning; SPA vite build OK. Commit 8049c54, đã push origin/main.
- 2026-09-23 | feat: ánh xạ cặp tem (GS1 Stamp Pair — Map_StampPair của InBrandCloud eTEM). Port từ InBrandCloud eTEM `Map_StampPair` (doc 09 §3.1 "Lưu mapping → QueSync_Map_StampPair", `Template.cs` Map_StampPair_Add/AddX + `Map_StampPair.cs` model + `SyncEtemNN.cs` Post_Lst_Map_StampPair + SQL `20210408.z11.CreateTable.Map_StampPair.sql`). Thêm entity `StampPair` (IDNo/BoxNo/NetworkId/PIN/QR_ID/QR_BoxNo/Remark/Active) + DbSet + query filter tenant + 2 unique index (IDNo, BoxNo), `TraceService.StampPairsAsync/SaveStampPairAsync/DeleteStampPairAsync` (áp 5 quy tắc InBrandCloud Map_StampPair_AddX: cần IDNo+BoxNo; IDNo phải tồn tại trong kho số tem Inv_InventoryGenID; BoxNo phải tồn tại trong kho số hộp Inv_InventoryGenBox; mỗi IDNo chỉ ghép 1 lần — chống trùng tem sản phẩm; mỗi BoxNo chỉ ghép 1 lần — chống trùng tem hộp), API `GET/POST /api/v1/stamp-pairs` + `DELETE /api/v1/stamp-pairs/{id}`, seed 2 cặp tem mẫu (P000001↔B2601010001, P000002↔B2601010002), trang SPA "Cặp tem" (list + form ghép/sửa/xóa). Build Release 0 error 0 warning; SPA vite build OK.
- 2026-09-23 | feat: danh mục vùng thị trường (GS1 Market Area — Mst_MarketArea). Port từ InBrandCloud eTEM `Mst_MarketArea` (doc 09 §2.1/§3, `Product/Product.cs` Mst_MarketArea_Save/SaveX + Mst_MarketArea_CheckDB + WAS_Mst_MarketArea_Get/Save + `MstMarketAreaController.cs` WA_Mst_MarketArea_Get/Save + `Mst_MarketArea.cs` model; dùng trong `SyncEtemNN.cs` join làm giàu "Vùng thị trường" cho bản ghi phân phối). Thêm entity `MarketArea` (Code/Name/AreaType/Description/Active) + DbSet + query filter tenant, `TraceService.MarketAreasAsync/SaveMarketAreaAsync/DeleteMarketAreaAsync` (áp quy tắc InBrandCloud: cần mã vùng MarketAreaCode + tên MarketAreaName — tương đương Mst_MarketArea_Save_InvalidMarketAreaCode; mã vùng duy nhất trong tenant — tương đương Mst_MarketArea_CheckDB theo OrgID+MarketAreaCode), API `GET/POST /api/v1/market-areas` + `DELETE /api/v1/market-areas/{id}`, seed 5 vùng thị trường mẫu (Miền Bắc/Trung/Nam, TP.HCM, ĐBSCL tạm ngưng), trang SPA "Vùng thị trường" (list + form thêm/sửa/xóa). Build Release 0 error 0 warning; SPA vite build OK.
- 2026-09-23 | feat: định danh sản phẩm (GS1 Product ID — Prd_ProductID của InBrandCloud ProductCenter). Port từ InBrandCloud `Prd_ProductID` (doc 09 §8.2 "dữ liệu phải truy xuất được" + §6.2 model ProductID_UI, `OS_MstProductCentrer.cs` OS_PrdCenter_Prd_ProductID_Create/Update/Delete/Get + `OS_PrdCenter_Prd_ProductID.cs` model + `OS_PrdCenter_Prd_ProductIDService.cs` WA_* + `OS_PrdCenter_Prd_ProductID.js` checkForm + `Views/OS_PrdCenter_Prd_ProductID/Create.cshtml`). Thêm entity `ProductId` (ProductID/SpecCode/ProductionDate/LotNo/BuyDate/SecretNo/WarrantyStartDate/WarrantyExpiredDate/WarrantyDuration/RefNo1-3+RefBiz1-3/Buyer/NetworkProductIdCode/Status/CustomField1-5/Remark) + enum `ProductIdStatus` (Ok/Ng/Repairing/Checking — tương đương OK/NG/REPAIRING/CHECKING) + DbSet + query filter tenant, `TraceService.ProductIdsAsync/GetProductIdAsync/SaveProductIdAsync/DeleteProductIdAsync` (áp quy tắc InBrandCloud: cần mã định danh ProductID — tương đương checkForm "Mã sản phẩm không được trống."; mã định danh duy nhất trong tenant — tương đương khóa OrgID+ProductID), API `GET/POST /api/v1/product-ids` + `GET/DELETE /api/v1/product-ids/{id}`, seed 3 định danh mẫu (1 OK còn bảo hành, 1 CHECKING, 1 REPAIRING), trang SPA "Định danh sản phẩm" (list + form thêm/sửa/xóa đầy đủ trường). Build Release 0 error 0 warning; SPA vite build OK. Commit b3aabd0, đã push origin/main.
- 2026-09-23 | feat: cấu hình trường hiển thị khi tra cứu (GS1 Mst_ConfigColumnSearch của InBrandCloud eTEM). Port từ InBrandCloud eTEM `Mst_ConfigColumnSearch` (doc 09 §6 báo cáo/tra cứu, `Master.cs` Mst_ConfigColumnSearch_CheckDB/Get/GetX + `Mst_ConfigColumnSearch.cs` model + `MstConfigColumnSearchController.cs` + dùng trong `Report.cs` Rpt_Inv_InventoryVerifiedIDForSearchX trả bảng Mst_ConfigColumnSearch). Thêm entity `ConfigColumnSearch` (CoumnID/TabID/TabName/NetworkId/TypeId/IdxInTab/ColumnDesc/FlagView/FlagOsOrgView/FlagShow/EsColumnId/EltsObjectId) + DbSet + query filter tenant + unique index bộ ba (CoumnID, NetworkId, TypeId), `TraceService.ConfigColumnSearchesAsync/SaveConfigColumnSearchAsync/DeleteConfigColumnSearchAsync` (áp quy tắc InBrandCloud Mst_ConfigColumnSearch_CheckDB: cần mã trường CoumnID + mã Tab TabID + loại bảng TypeID; bộ ba (CoumnID, NetworkID, TypeID) duy nhất trong tenant — chống trùng cấu hình), API `GET/POST /api/v1/config-column-searches` + `DELETE /api/v1/config-column-searches/{id}`, seed 8 cấu hình mẫu (2 tab: Thông tin sản phẩm / Hành trình truy xuất / Lịch sử phân phối, có cờ hiển thị trong/ngoài Org), trang SPA "Cấu hình trường tra cứu" (list + form thêm/sửa/xóa). Build Release 0 error 0 warning; SPA vite build OK.- 2026-09-23 | feat: sản phẩm đã sản xuất / dãy sản xuất (GS1 Inv_InventoryManufacturedID của InBrandCloud eTEM). Port từ InBrandCloud eTEM `Inv_InventoryManufacturedID` (doc 09 §3.1 "Sinh tem khi nhập kho" + §2.2 chuỗi cung ứng, `Template.cs` Inv_InventoryManufacturedID_AddMulti/AddMultiX + `Inv_InventoryManufacturedID.cs` model). Thêm entity `ManufacturedId` (IDNo/IManufacturedIDNo/NetworkId/BoxNo/LineCode/LineRootCode/ShiftCode/ProductionLotNo/RefNoLine/ProductCode/InvCode/ManufactureStartDTime/MobileScanDTime/MobileIndex/FlagMap/Status/CompleteDTime/CompleteBy/Remark) + enum `ManufacturedStatus` (InProgress/Completed — tương đương FlagCompleted) + DbSet + query filter tenant + unique index (OrgId, IDNo), `TraceService.ManufacturedIdsAsync/SaveManufacturedIdAsync/DeleteManufacturedIdAsync` (áp 3 quy tắc InBrandCloud Inv_InventoryManufacturedID_AddMultiX: cần mã tem IDNo + mã dãy sản xuất IManufacturedIDNo; IDNo phải tồn tại trong kho số tem Inv_InventoryGenID; IDNo chưa được ghi nhận trong dãy sản xuất nào — chống trùng, mỗi tem chỉ sản xuất 1 lần), API `GET/POST /api/v1/manufactured-ids` + `DELETE /api/v1/manufactured-ids/{id}`, seed 3 bản ghi mẫu (2 tem hoàn tất trên dây chuyền A1 ca 1, 1 tem đang sản xuất trên dây chuyền A2 ca 2), trang SPA "Dãy sản xuất" (list + form ghi nhận/sửa/xóa). Build Release 0 error 0 warning; SPA vite build OK.

- 2026-09-23 | feat: danh mục mạng lưới / môi trường (GS1 Network Master — MstSv_Mst_Network của InBrandCloud eTEM). Port từ InBrandCloud eTEM `MstSv_Mst_Network` (doc 09 §5 "Đồng bộ với eTEM server", `MasterServer.cs` MstSv_Mst_Network_GetByMST/Get/Create + MstSv_Mst_Network_CheckDB + `MstSvMstNetworkController.cs` WA_MstSv_Mst_Network_GetByMST/Add/Gen + `MstSv_Mst_Network.cs` model + `SyncEtemNN.cs` MstSv_Mst_Network_SyncToELTSX/Post_Lst_MstSv_Mst_Network). Thêm entity `NetworkMaster` (NetworkID/NetworkName/GroupNetworkID/CoreAddr/PingAddr/XSysAddr/WSUrlAddr/WSUrlAddrNew/DBUrlAddr/Mst/OrgIdSln/MinVersion/Active/Remark) + DbSet + query filter tenant + unique index (OrgId, NetworkID), `TraceService.NetworkMastersAsync/SaveNetworkMasterAsync/DeleteNetworkMasterAsync` (áp quy tắc InBrandCloud MstSv_Mst_Network_Create + MstSv_Mst_Network_CheckDB: cần mã mạng NetworkID — tương đương MstSv_Mst_Network_Create_InvalidNetworkID; cần tên mạng NetworkName — tương đương MstSv_Mst_Network_Create_InvalidNetworkName; mã mạng duy nhất trong tenant — tương đương MstSv_Mst_Network_CheckDB NetworkExist), API `GET/POST /api/v1/network-masters` + `DELETE /api/v1/network-masters/{id}`, seed 3 mạng mẫu (mạng sản xuất chính Real 4341766000, mạng Bình Điền 9452386000, mạng test tạm ngưng), trang SPA "Mạng lưới" (list + form thêm/sửa/xóa đầy đủ trường). Build Release 0 error 0 warning; SPA vite build OK. Commit 42c2911, đã push origin/main.
- 2026-09-23 | feat: lịch sử phân phối / nhập-xuất kho theo tem (GS1 Distribution History — InvF_InventoryHistInOutID của InBrandCloud eTEM). Port từ InBrandCloud eTEM `InvF_InventoryHistInOutID` (doc 09 §2.1 chuỗi cung ứng + §6 báo cáo/tra cứu, `InvGen.cs` InvF_InventoryHistInOutID: Save nhánh RefType=IN/OUT + `Template.cs`/`Report.cs` join sang Mst_MarketArea/Mst_Customer/Mst_Province để làm giàu vùng thị trường + khách hàng). Thêm entity `DistributionHistory` (IF_InvInHistNo/IDNo/NetworkId/RefNo/RefType/IF_InvInNo/InvCode/ProductionDate/PackageDate/ProductionLotNo/ShiftInCode/PrintDate/BoxNo/CanNo/IF_InvOutNo/InvFOutType/OrgID_Customer/OrgID_CustomerCurr/CustomerCode/CustomerName/PlateNo/MoocNo/DriverName/DriverPhoneNo/UserKCS/AreaName/IVerifiedIDInOutNo/CustomerCodeSysDelivery/CustomerNameDelivery/AreaCodeDelivery/FlagIsError/Remark) + enum `HistRefType` (In/Out — tương đương RefType IN/OUT) + DbSet + query filter tenant + unique index (OrgId, IF_InvInHistNo) + index (OrgId, IDNo), `TraceService.DistributionHistoriesAsync/SaveDistributionHistoryAsync/DeleteDistributionHistoryAsync` (áp quy tắc InBrandCloud: cần mã tem IDNo; tem phải tồn tại trong kho số tem Inv_InventoryGenID; mã bản ghi lịch sử duy nhất trong tenant; vùng thị trường AreaName nếu có phải tồn tại trong danh mục Mst_MarketArea; join sang Mst_MarketArea làm giàu tên vùng thị trường), API `GET /api/v1/distribution-histories` (lọc theo chiều IN/OUT) + `POST` + `DELETE /api/v1/distribution-histories/{id}`, seed 4 bản ghi mẫu (1 nhập kho + 2 xuất kho tới đại lý/điểm bán lẻ + 1 xuất kho ghép tem lỗi), trang SPA "Lịch sử phân phối" (list + lọc chiều + form thêm/sửa/xóa đầy đủ trường). Build Release 0 error 0 warning; SPA vite build OK.
- 2026-09-23 | feat: danh mục đại lý / đơn vị phân phối (GS1 Dealer — Mst_Dealer của InBrandCloud). Port từ InBrandCloud `Mst_Dealer` (doc 09 §2.1 chuỗi cung ứng + §6 báo cáo/tra cứu, `Master.cs` Mst_Dealer_CheckDB + `Mst_Dealer.cs` model + `Mst_DealerController.cs` Create/Edit/Delete + dùng trong `eTemNN.cs` Mst_NNT_Create và `RptSvLocaleTemNN.cs` — kiểm tra mã đại lý DLCode phải tồn tại và đang hoạt động). Thêm entity `Dealer` (DLCode/DLCodeParent/NetworkId/DLBUCode/DLBUPattern/DLLevel/ProvinceCode/DLName/DLType/DLAddress/DLPresentBy/DLGovIDNumber/DLEmail/DLPhoneNo/Active/Remark) + DbSet + query filter tenant + unique index (OrgId, DLCode), `TraceService.DealersAsync/SaveDealerAsync/DeleteDealerAsync` (áp quy tắc InBrandCloud Mst_Dealer_CheckDB: cần mã đại lý DLCode + tên DLName; mã đại lý duy nhất trong tenant — tương đương Mst_Dealer_CheckDB_DLCodeExist), API `GET/POST /api/v1/dealers` + `DELETE /api/v1/dealers/{id}`, seed 3 đại lý mẫu (đại lý cấp 1 TP.HCM + Cần Thơ đang hoạt động, đại lý cấp 2 Hà Nội tạm ngưng), trang SPA "Đại lý" (list + form thêm/sửa/xóa đầy đủ trường). Build Release 0 error 0 warning; SPA vite build OK.
